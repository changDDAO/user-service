services:
  app:

    container_name: user-service-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8082:8082"
    networks:
      - msa-net


networks:
  msa-net:
    external: true
